Hover/focus label in a small dark glass bubble. Wraps one child; positions above by default.

```jsx
<Tooltip label="Duplicate" side="top">
  <Button variant="ghost" size="sm">⌘D</Button>
</Tooltip>
```

`side`: `top` (default) · `bottom` · `left` · `right`.
